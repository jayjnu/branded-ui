# branded-ui
Define and enforce UI component roles with framework-agnostic, compile-time brands.

## Examples

Example projects follow `examples/$stack-$build-tool-$linter` so framework adapters and linter integrations can be tested independently.
